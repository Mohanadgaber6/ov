{
  "version": 2,
  "buildCommand": "pnpm -r --if-present run build",
  "outputDirectory": "artifacts/ov-office-site/dist/public",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
